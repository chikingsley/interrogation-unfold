# Analysis

Use this directory for derived indexes, reports, and hand-written notes about the
checked-in corpus.

The first reusable inventory command is:

```sh
uv run interrogation-unfold inspect
```

Keep raw recovered files in `corpus/` or `raw/`; use `analysis/` for derived
tables, indexes, and notes that explain the corpus.
